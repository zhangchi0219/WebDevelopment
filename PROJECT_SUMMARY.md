# 3D 照片画廊系统 — 项目交付说明书

## 一、项目背景与目标

本项目旨在为用户提供一套沉浸式的在线照片展示解决方案。区别于传统的平面瀑布流或网格式相册，本系统采用 WebGL 三维渲染技术，将照片以立体圆形阵列的形式呈现在浏览器中，为访问者带来具有视觉冲击力的浏览体验。

系统同时具备完整的照片管理能力，支持上传、预览、删除等操作，开箱即用，无需额外安装客户端软件，通过浏览器即可访问全部功能。

## 二、已交付功能清单

### 1. 照片上传

- 支持单张上传与批量上传（单次最多 20 张）。
- 支持拖拽文件至页面直接上传。
- 上传过程中提供实时进度条反馈。
- 支持 JPEG、PNG、GIF、WebP 四种主流图片格式，单张文件大小限制为 10MB。
- 上传完成后照片自动进入 3D 画廊展示，无需刷新页面。

### 2. 3D 画廊展示

- 所有照片在三维空间中以圆形阵列自动排列，布局根据照片数量动态调整，始终保持美观的视觉比例。
- 每张照片具有圆形裁剪效果，加载时带有平滑淡入动画。
- 场景背景为星空粒子效果，增强沉浸感。
- 画廊默认自动缓慢旋转，营造动态展示氛围。

### 3. 交互操作

- 鼠标左键拖拽：旋转画廊视角。
- 鼠标滚轮：缩放远近。
- 鼠标右键拖拽：平移画面。
- 用户操作时自动旋转暂停，静止 3 秒后自动恢复。
- 单击任意照片可弹出全屏灯箱查看原图。

### 4. 照片管理

- 支持删除单张照片。
- 支持一键清空全部照片。
- 界面实时显示照片数量与存储空间占用统计。

### 5. 界面与体验

- 采用毛玻璃（Glassmorphism）设计风格，界面现代美观。
- 上传面板可折叠收起，不遮挡画廊主体。
- 所有操作提供即时 Toast 消息提示（成功/失败）。
- 页面底部展示交互操作引导说明。
- 深色主题配色，突出照片本身的视觉表现力。

## 三、技术架构概览

本系统采用前后端分离架构，整体分为三个层次：

**展示层（前端）**：基于 Three.js 3D 引擎实现画廊渲染与用户交互，使用原生 JavaScript 开发，无第三方前端框架依赖，加载速度快，兼容性好。

**服务层（后端）**：基于 Node.js + Express 搭建，提供标准 RESTful 接口，负责照片的接收、存储、查询与删除等业务逻辑。

**数据层**：采用 SQLite 轻量级数据库，记录照片元数据（文件名、格式、大小、上传时间等），无需额外部署数据库服务，降低运维成本。

## 四、系统接口说明

系统后端共提供以下六个接口，供前端页面或后续系统对接使用：

| 功能 | 请求方式 | 接口路径 |
|------|----------|----------|
| 上传单张照片 | POST | /api/photos |
| 批量上传照片 | POST | /api/photos/batch |
| 获取照片列表 | GET | /api/photos |
| 删除指定照片 | DELETE | /api/photos/:id |
| 清空全部照片 | DELETE | /api/photos/all |
| 获取统计信息 | GET | /api/photos/stats |

## 五、部署与运行

系统部署步骤简单，仅需安装 Node.js 环境后执行以下操作即可启动：

1. 安装项目依赖。
2. 启动服务，默认监听 3000 端口。
3. 通过浏览器访问即可使用全部功能。

数据库与上传目录在首次启动时自动创建，无需手动配置。

## 六、项目总结

本项目已完成全部预定功能的开发与交付，系统运行稳定，功能完整。3D 展示效果流畅，交互体验自然，界面设计现代美观。系统架构简洁清晰，便于后续维护与功能扩展。如需增加用户认证、相册分类、分享链接等功能，可在现有架构基础上快速迭代。
