# 项目总结：3D 照片画廊 Web 应用

## 项目概述

这是一个基于 Three.js 的 3D 照片画廊 Web 应用。用户可以上传照片，系统会将照片以圆形阵列的方式排列在三维空间中，用户可以通过鼠标交互（旋转、缩放、平移）来浏览照片，点击照片可查看大图。

## 技术选型

后端采用 Express.js 作为 Web 框架，使用 better-sqlite3 作为轻量级数据库存储照片元数据，Multer 处理文件上传。前端没有使用任何前端框架，采用原生 JavaScript 开发，核心 3D 渲染能力由 Three.js 提供，配合 OrbitControls 实现交互控制。整体架构简洁，前后端通过 RESTful API 通信。

## 核心功能

### 照片上传

支持单张上传和批量上传（最多 20 张），也支持拖拽上传。上传时有进度条反馈，限制文件类型为 JPEG、PNG、GIF、WebP，单张不超过 10MB。上传的文件使用 UUID 重命名以避免冲突，元数据（文件名、大小、类型等）存入 SQLite 数据库。

### 3D 画廊展示

照片被加载为纹理贴图，贴在平面几何体上，按照动态计算的网格排列成圆形阵列。圆形的大小根据照片数量自动调整，算法基于 `sqrt(4 * 照片数量 / π)` 来确定最优的网格尺寸。每张照片使用自定义 GLSL 着色器实现圆形裁剪效果，加载完成后有淡入动画。场景背景包含一个由 1000 个粒子组成的星空效果。

### 交互控制

鼠标左键拖拽旋转视角，滚轮缩放远近，右键平移画面。场景默认启用自动旋转，用户操作时暂停，静止 3 秒后恢复。点击任意照片可在灯箱（lightbox）覆盖层中查看原图。

### 照片管理

提供删除单张照片和清空全部照片的功能。界面上实时显示照片数量统计。后端 API 也提供照片统计接口，返回照片总数和占用空间。

## 界面设计

采用毛玻璃（glass-morphism）风格，上传面板可折叠收起以减少视觉干扰。操作反馈通过 Toast 通知呈现，页面底部有控制提示说明交互方式。整体配色偏暗，突出照片本身的视觉效果。

## 后端 API

提供六个接口：上传单张照片、批量上传、获取照片列表（支持数量限制参数）、删除指定照片、删除全部照片、获取统计信息。所有照片文件存放在 `public/uploads/` 目录下，通过静态文件服务访问。

## 数据存储

使用 SQLite 数据库，单表结构，记录每张照片的 UUID 主键、存储文件名、原始文件名、MIME 类型、文件大小和创建时间。数据库文件在首次启动时自动创建。

## 总结

这个项目是一个将照片管理与 3D 可视化结合的 Web 应用，技术上体现了对 Three.js 3D 渲染、GLSL 着色器编程、Node.js 后端开发以及前端交互设计的综合运用。项目结构清晰，前后端分离，代码量适中，是一个功能完整的全栈 Web 应用。
